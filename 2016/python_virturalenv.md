# How to use Python Virturalenv?

Usage:

```bash
$ virtualenv /path/to/ENV  # create
$ source /path/to/ENV      # activate

$ deactivate               # deactivate
$ rm -r /path/to/ENV       # delete
```

Refer:

- https://virtualenv.readthedocs.org/en/latest/userguide.html

